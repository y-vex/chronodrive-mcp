[project]
name = "chronodrive-mcp"
version = "0.1.0"
description = "MCP server for Chronodrive grocery cart automation"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
]

[project.scripts]
chronodrive-mcp = "server:main"
